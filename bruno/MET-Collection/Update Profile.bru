meta {
  name: Update Profile
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/users/profile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "email": "updated_testuser@example.com"
  }
}
