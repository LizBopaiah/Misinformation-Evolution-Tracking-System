meta {
  name: Update Profile
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/users/profile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "full_name": "Updated Test User"
  }
}

tests {
  test("Profile update should be successful", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
